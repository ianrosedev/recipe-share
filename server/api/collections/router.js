import { Router } from 'express';
const router = Router();

router.route('/')
  .get(/* TODO */);

router.route('/:id')
  .get(/* TODO */)
  .post(/* TODO */)
  .put(/* TODO */)
  .delete(/* TODO */);

export default router;
