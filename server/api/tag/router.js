import { Router } from 'express';
const router = Router();

router.route('/')
  .get(/* TODO */)
  .post(/* TODO */);

router.route('/:id')
  .get(/* TODO */)
  .delete(/* TODO */);

export default router;
